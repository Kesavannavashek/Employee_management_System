import "../globals.css";
export const metadata={
    title:'Register',
    descrption:'about Register'
}

export default function Layout({children}){

    return(<>
        
        <main>{children}</main>
        </>
    )
    
} 