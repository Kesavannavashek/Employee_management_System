import React from 'react'

export const metadata={
    title:'Login',
    description:'It is about Login Page'
}

const Layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default Layout